import { CapacitorConfig } from "@capacitor/cli";

const config: CapacitorConfig = {
  appId: "com.joao.test.app",
  appName: "teste joao",
  webDir: "out",
  server: {
    androidScheme: "https",
  },
};

export default config;
